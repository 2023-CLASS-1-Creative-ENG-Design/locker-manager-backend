version: '3'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: locker-manager-backend
    depends_on:
      - db
    links:
      - db:locker_manager_db
    ports:
      - "43001:8080"
    restart: always

  db:
    image: mysql/mysql-server:8.0
    container_name: locker-manager-mysql
    environment:
      MYSQL_DATABASE: PyJGKBcEaamwXQYH6sXs_db
      MYSQL_USER: mRMnzx3tHZhTZxfiNJ75_user
      MYSQL_PASSWORD: GjMcGwqz7E3B88CgDwTfrC3GaYnfQf
      TZ: 'Asia/Seoul'
    restart: always
    volumes:
      - locker-manager-mysql:/var/lib/mysql

volumes:
  locker-manager-mysql: